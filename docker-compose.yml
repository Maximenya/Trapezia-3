version: "2"
services:
  trapezia:
    container_name: trapezia
    restart: always
    build: .
    ports:
      - "3000:3000"
    links:
      - mongo
  mongo:
    container_name: mongo
    image: mongo
    volumes:
      - ./data:/data/usersdb
    ports:
      - "27017:27017"